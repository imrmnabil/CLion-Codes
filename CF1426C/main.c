#include <stdio.h>

int main() {
    int t, a = 1, sum = 1, n, count = 0;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        while(1)
        {
            sum = sum + a;
            count++ ;
            if(sum >= n)
            {
                break;
            }
            sum = 
        }
    }
    return 0;
}
